name: 🔵 Blue Notes — Build & Release APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  DOTNET_VERSION: '8.0.x'

jobs:
  build-android:
    name: 📦 Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: 🛠️ Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 📲 Install MAUI Android workload
        run: dotnet workload install maui-android

      - name: 📦 Restore & Build APK
        run: |
          # Busca automática do arquivo de projeto para evitar erro de caminho
          PROJECT_FILE=$(find . -name "*.csproj" -print -quit)
          
          if [ -z "$PROJECT_FILE" ]; then
            echo "❌ ERRO: Nenhum arquivo .csproj encontrado no repositório!"
            exit 1
          fi
          
          echo "🚀 Projeto encontrado: $PROJECT_FILE"
          
          dotnet restore "$PROJECT_FILE"
          
          dotnet publish "$PROJECT_FILE" \
            -f net8.0-android \
            -c Release \
            -p:AndroidPackageFormat=apk \
            -p:AndroidKeyStore=false \
            -p:EmbedAssembliesIntoApk=true \
            -o ./artifacts/

      - name: 📤 Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: BlueNotes-APK
          path: ./artifacts/**/*.apk
          retention-days: 30
          
